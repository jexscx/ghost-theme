{{!< default}}

<div class="content-area">
    <main class="site-main container">
        {{> featured}}
        {{#get "tags" limit="all" include="count.posts"}}
            <div class="topic-feed">
                <div class="row">
                    {{#foreach tags}}
                        <div class="col-md-6">
                            <section class="topic">
                                <header class="topic-header">
                                    <h2 class="topic-name">{{name}}</h2>
                                    <div class="topic-count">
                                        {{plural count.posts empty="Geen bijlagen beschikbaar" singular="1 bijlage" plural="% bijlagen"}}
                                    </div>
                                </header>
                                {{#get "posts" order="published_at asc" filter="tag:{{slug}}"
                                    limit="20" as |featured|}}
                                    <ul class="topic-article-feed u-plain-list">
                                        {{#foreach featured}}
                                            <li class="topic-article">
                                                <h3 class="topic-article-title">
                                                    <a class="topic-article-link" href="{{url}}">
                                                        <i class="topic-article-icon icon icon-chevron-right"></i>{{title}}
                                                    </a>
                                                </h3>
                                            </li>
                                        {{/foreach}}
                                    </ul>
                                {{/get}}
                                <a class="topic-more button-arrow button-arrow-right" href="{{url}}">Bekijk alle bijlagen</a>
                            </section>
                        </div>
                    {{/foreach}}
                </div>
            </div>
        {{/get}}
    </main>
</div>